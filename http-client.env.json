{
  "_comment": "Template environnement public (committed). Les secrets sont dans http-client.private.env.json (gitignored) — généré par scripts/regen-jwt-env-enterprise.sh.",
  "dev": {
    "base": "http://localhost:8083",
    "jwtSecret": "(unused — dev mode permitAll, no JWT needed)",
    "adminUserId": "(unused)"
  },
  "oauth2": {
    "base": "http://localhost:8083",
    "jwtSecret": "(set in http-client.private.env.json, gitignored)",
    "adminUserId": "(set in http-client.private.env.json, gitignored)"
  }
}
