package com.onesley.oneclick.entity.shared;

/**
 * Enum {@code public.distribution_source} (généré par scripts/scaffold-jpa.mjs).
 */
public enum DistributionSource {
    wallet_admin,
    restaurant_credit,
    bonus_system,
    recycled_expired
}
